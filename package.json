{
  "name": "ai-agent",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '@ai-agent/*' dev",
    "build": "bun run --filter '@ai-agent/*' build",
    "test": "bun run --filter '@ai-agent/*' test",
    "lint": "bun run --filter '@ai-agent/*' lint",
    "typecheck": "bun run --filter '@ai-agent/*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
